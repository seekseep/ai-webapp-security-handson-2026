services:
  app:
    container_name: handson-202605-02-auth-03-broken-authorization
    build: .
    ports:
      - "3000:3000"
    environment:
      - DB_PATH=/workspace/data/database.sqlite
    volumes:
      - ./app:/workspace/app
      - ./scripts:/workspace/scripts
      - ./data:/workspace/data
      - /workspace/node_modules
